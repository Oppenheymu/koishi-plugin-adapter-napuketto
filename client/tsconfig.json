{
    "compilerOptions": {
        "rootDir": ".",
        "module": "esnext",
        "moduleResolution": "bundler",
        "skipLibCheck": true,
        // 不引用 @koishijs/client/global：其 5.30.11 exports 把 ./global 映射到
        // 不存在的 ./client/global.d.ts（上游打包 bug，最新版未修）。vue 模块声明
        // 由同目录 shims.d.ts 提供。
        "types": [],
    },
    "include": [
        "."
    ],
}